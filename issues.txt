iPhone
	Safari
		gelöst:
			Es kommt die Meldung: eTuneBook cannot import BoxPlayer_V01.abc due to: Error: QUOTA_EXCEEDED_ERR:DOM Exception 22. Aber BoxPlayer_V01.abc kann trotzdem geladen werden. Lösung: iphone: privates surfen = nein -> localstorage funktioniert, DOM-Exception kommt nicht mehr.
		ungelöst:
			Move-Button eine Zeile runtergerutscht.
			Played button drücken: es kommt 'a few second ago', aber der Button bleibt blau. Auch beim wegclicken des Info-Editors: Button bleibt blau.
			Info Editor: Label sind nicht links sondern oben am Feld. -> Site 2,3 und YouTube 2,3 sind nur mit 2,3 gelabeled -> sieht nicht gut aus, wenn oben am Feld.
			Filter -> sampledots werden nicht aktualisiert (erst nachddem zum Beispiel auf den Info-Button zweimal gedrückt wurde)
			Auf dem kleinen Bildschirm ist irreführend wenn nach einem Filter nichts angezeigt wird, weil Window-Position nicht nachgezogen wird.
			Kästchen mit Button noch weiter bis auf Titel verkleinern? (sampledots sind zu klein im Vergleich zu den tune-Kästen). Oder Modus mit nur sampledots (inkl. titel)? 
		Ansonsten: sehr gut (Noten werden angzeigt, filter funktioniert...)
	Delphin
		Colorizer rutscht eine Zeile nach unten
	
Allmemein
	Gelöst:
		Nach Abc-Editierung verschwindet die Anzeige der ersten Takte. Lösung: in der Aufbereitung des Abc für die sampledots (title entfernen, erste Takte) wurde beim wieder zusammenschustern ein /n vergessen (renderAbc braucht strikt getrennte Linien, meldet aber nichts, wenn's nicht ok ist). Offensichtlich kommt dies aber nur zum tragen nach Editierung in der Textarea. Grund unklar.
		Link zum Bild hinter eTuneBook-Projekt-Name richtig gestellt.
		Appcache-Einführung -> wirklicher Offline-Betrieb möglich. 
		Fingering On/Off war zwischenzeitlich ausser Betrieb.
		humours-of-whiskey-set: letztes tune hat als einziges zwei zeilen. warum?: Lösung: siehe BoxplayerV01: A-Teil und B-Teil verschiedene Keys.
		Hide fingering im sampledots (zum beispiel wenn alle tune-panels (rehearse, info und edit) geschlossen sind (sonst kommt fingering in nächst oberes tune)
	Ungelöst:
		Nach Fingering-Anpassung vom 11.10: ABC-Button -> Tune Select -> Texteditor geht auf mit noten rechts, sampledots der anderen tunes verschwinden aber nicht. zweites mal auf abc-button -> verschwunden. Initialisierungs-Problem.
		Zweimal hintereinander Open geht nicht
		Springen des Bildschirms nach oben, nachdem man bei längeren tunes nach unten navigiert hat, um gleichzeitig die noten zu sehen. Editierung mühsam. 
		Zweimal hintereinander ramdom sort geht nicht.
	Neue Features:	
		Hide chords
		ev. MIDI einbauen
		Vergrösserungsfunktion für Sampledots? Angabe wieviele Takte angezeigt werden sollen? Ev. im Zusammenhang mit Show only sampledots? (Session-Modus auf iPhone). Oder beim wegklicken von den button-lines die Grösse anpassen?
		Dots drucken
		Eher als Vergrösserung für Sampledots: Settings speichern (hide info-  und edit panel -> mit Browser vergrössern -> Gute Anzeige.
		