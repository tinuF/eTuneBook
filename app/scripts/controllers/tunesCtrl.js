'use strict';

/**
 * Controller for info Templates
 */
angular.module('eTuneBookApp').controller( 'tunesCtrl', function ( $scope, $location, $timeout, $rootScope, $state, eTuneBookService ) {
    $scope.playDateFilter = "All Tunes";
});
